<Page x:Class="Pract34.EntityFramework.OdnaTable2E"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pract34.EntityFramework"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OdnaTable2E">

    <Grid>
        <DataGrid x:Name="ProductsPageGrid" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding ProductName}" Header="Имя продукта"/>
                <DataGridTextColumn Binding="{Binding Price}" Header="Цена"/>
                <DataGridTextColumn Binding="{Binding Categories.CategoriiName}" Header="Категории"/>
                <DataGridTextColumn Binding="{Binding Country.CountryName}" Header="Страна производитель"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
